{
  "name": "microreq",
  "version": "0.12.0",
  "description": "tiny convenience wrapper around http.request",
  "main": "index.js",
  "author": "Andras",
  "license": "Apache-2.0",
  "repository": "https://github.com/andrasq/node-microreq#readme",
  "keywords": [ "http", "request", "small", "quick", "minimal", "thin", "light" ],
  "engines": { "node": ">= 0.11" },

  "dependencies": {
  },
  "devDependencies": {
    "qmock": "0.16.0"
  },

  "scripts": {
    "test": "qnit test.js",
    "coverage": "nyc --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output coverage"
  }
}
